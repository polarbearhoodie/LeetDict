# The assignment operator = is different
# typically, we use = to assign values, 
# and == to check equality
var_x = 2

my_str = "a string"

a_cast_str = str(3)

# Notice that even though we did not inculde a newline
# operatior, typically written "\n", it included it for us!
# very useful for debugging
print(my_str)
print(a_cast_str)
